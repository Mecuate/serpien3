import { keyframes } from 'stitches.conf'

export const videoControlsFadeOutSlow = keyframes({
    '0%': { opacity: 1 },
    '10%': { opacity: 0.88 },
    '70%': { opacity: 0.8 },
    '85%': { opacity: 0.5 },
    '100%': { transform: 0 },
})
