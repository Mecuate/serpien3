import { styled } from 'stitches.conf'

export const Container = styled('div', {
    display: 'flex',
    position: 'relative',
    overflow: 'auto',
    width: '100%',
    height: '100%',
    margin: '$4',
})
