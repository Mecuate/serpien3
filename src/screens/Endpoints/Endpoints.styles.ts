import { styled } from 'stitches.conf'

export const Container = styled('div', {
    display: 'block',
    position: 'relative',
    padding: 1,
    margin: 0,
    overflow: 'auto',
    width: '100%',
    height: '100%',
})
