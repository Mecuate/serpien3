import { Screen } from 'components/Screen'
import { APP_PATH } from 'models'

export const LandingScreen = () => {

    return <Screen pannel={APP_PATH.LANDING}>
        <div>
            landing page
        </div>
    </Screen>
}